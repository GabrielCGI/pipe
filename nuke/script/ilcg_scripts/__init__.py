from . import retrack_nodes


def retrack_every_nodes():
    print("Retrack every nodes")
    retrack_nodes.retrack_every_nodes()


def retrack_selected_nodes():
    print("Retrack selected nodes")
    retrack_nodes.retrack_selected_nodes()