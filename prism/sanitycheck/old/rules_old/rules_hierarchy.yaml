---
# Rule hierarchy yaml
Production:
  rules:
    - TestCheck
  Assets:
    rules:
    Modeling:
      rules:
        - testFalse
      Modeling:
        rules:
          - have_shader_group_well_named
    Shading:
      rules:
      Lookdev:
        rules:
          - have_material_preview
  Shots:
    rules:
    Lighting:
      rules:
      Lighting SEQ:
        rules:
          - have_all_render_node_set_to_xpu
