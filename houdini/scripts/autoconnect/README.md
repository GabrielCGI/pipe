# Autoconnect

Autoconnect is a Houdini tool made to simplify creation of materialX shaders.

## Installation

Place this package somewhere is in the `PYTHONPATH` scope.

## Usage

```python
import autoconnect

autoconnect.main()
```